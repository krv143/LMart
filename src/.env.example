# The New Orders / Orders Map / Pick an Order pages already show an interactive map
# (OpenStreetMap) with all order pins and no key — this is OPTIONAL for those, only needed if you
# want Google's own map instead. It IS REQUIRED for the DeliveryTracking live turn-by-turn page,
# which uses the Google Maps JS API directly. Then run npm run cap:sync.
REACT_APP_GOOGLE_MAPS_API_KEY="AIzaSyDpcQs7a9Gl4cX5UKJeHFcvgvBCHlv6SAs"
